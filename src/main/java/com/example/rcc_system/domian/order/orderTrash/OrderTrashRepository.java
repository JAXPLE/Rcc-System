package com.example.rcc_system.domian.order.orderTrash;

import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderTrashRepository extends JpaRepository<OrderTrash,Integer> {
}
